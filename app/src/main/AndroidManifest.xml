<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="alvi17.mathformula" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".activity.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.AlgebraActivity"/>
        <activity android:name=".activity.FunctionsFormulasActivity"/>
        <activity android:name=".activity.Geometry"/>
        <activity android:name=".activity.Limit"/>
        <activity android:name=".activity.IndifiniteIntegrals"/>
        <activity android:name=".activity.DefiniteIntegrals" />
        <activity android:name=".activity.Series"/>
    </application>

</manifest>
